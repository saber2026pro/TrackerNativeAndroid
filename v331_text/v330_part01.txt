inmax(0,1fr))}.topbar .v203-logout{display:inline-flex!important}}
`;
document.head.appendChild(v330Style);

/* record reminder UI */
function v330PresetLabel(k){return ({default:'استخدام النمط الافتراضي',none:'بدون تنبيه',due:'يوم الموعد',day_before_due:'قبل يوم + يوم الموعد',three_days_due:'قبل 3 أيام + يوم الموعد',week_due:'قبل أسبوع + يوم الموعد'}[k]||k)}
function v330ReminderFormHtml(f){
 f.reminderPreset=f.reminderPreset||'default';f.customReminders=Array.isArray(f.customReminders)?f.customReminders:[];
 return `<div class="field span2"><label>التنبيهات</label><div class="v330-reminder-box"><div class="field"><label>نمط التنبيه لهذا العمل</label><select class="select" data-v330-reminder-preset>${['default','none','due','day_before_due','three_days_due','week_due'].map(k=>`<option value="${k}" ${f.reminderPreset===k?'selected':''}>${v330PresetLabel(k)}</option>`).join('')}</select></div><div class="v330-reminders">${f.customReminders.map((r,i)=>`<div class="v330-reminder-row"><div class="field"><label>تاريخ التنبيه المخصص</label><input class="input" type="date" data-v330-reminder-date="${i}" value="${esc(r.date||'')}"></div><div class="field"><label>الوقت</label><input class="input" type="time" data-v330-reminder-time="${i}" value="${esc(r.time||state.settings.defaultReminderTime||'09:00')}"></div><button type="button" class="btn danger small" data-action="v330-remove-reminder" data-index="${i}">×</button></div>`).join('')}</div><div><button type="button" class="btn secondary small" data-action="v330-add-reminder">${icon('plus')}إضافة تنبيه آخر</button></div></div></div>`
}
const v330OpenNewBase=openNew;openNew=function(type=''){v330OpenNewBase(type);if(state.form){state.form.reminderPreset='default';state.form.customReminders=[];render()}};
const v330OpenEditBase=openEdit;openEdit=function(id){v330OpenEditBase(id);if(state.form){const r=state.records.find(x=>x.id===id);state.form.reminderPreset=r?.reminderPreset||'default';state.form.customReminders=clone(r?.customReminders||[]);render()}};
const v330RenderFormBase=renderForm;renderForm=function(){if(!state.form)return v330RenderFormBase();let html=v330RenderFormBase();const marker='<div class="field span2"><label>المرفقات</label>';return html.replace(marker,v330ReminderFormHtml(state.form)+marker)};
const v330SaveFormBase=saveForm;saveForm=async function(e){
 const editId=state.editingId,beforeUid=editId?state.records.find(r=>r.id===editId)?.uid:null,preset=state.form?.reminderPreset||'default',custom=clone(state.form?.customReminders||[]),actor=v330Actor();
 await v330SaveFormBase(e);if(state.modal==='form')return;
 let r=beforeUid?state.records.find(x=>x.uid===beforeUid):[...state.records].sort((a,b)=>(Number(b.seq)||0)-(Number(a.seq)||0))[0];if(!r)return;v330EnsureRecord(r);r.reminderPreset=preset;r.customReminders=custom;r.updatedByUserId=actor.id;if(!r.creatorUserId)r.creatorUserId=actor.id;r.createdByUserId=r.creatorUserId;for(const p of r.progress||[]){if(!p.userId&&p.user===actor.name)p.userId=actor.id}await saveExcel('تم حفظ العمل والتنبيهات');
};

document.addEventListener('change',e=>{const t=e.target;if(t.matches('[data-v330-reminder-preset]')&&state.form){state.form.reminderPreset=t.value}else if(t.matches('[data-v330-reminder-date]')&&state.form){const i=Number(t.dataset.v330ReminderDate);if(state.form.customReminders[i])state.form.customReminders[i].date=t.value}else if(t.matches('[data-v330-reminder-time]')&&state.form){const i=Number(t.dataset.v330ReminderTime);if(state.form.customReminders[i])state.form.customReminders[i].time=t.value}},true);
document.addEventListener('click',e=>{const b=e.target.closest?.('[data-action]');if(!b)return;if(b.dataset.action==='v330-add-reminder'&&state.form){e.preventDefault();e.stopImmediatePropagation();state.form.customReminders.push({rid:v2Uid('REM'),date:'',time:state.settings.defaultReminderTime||'09:00',enabled:'نعم'});render()}else if(b.dataset.action==='v330-remove-reminder'&&state.form){e.preventDefault();e.stopImmediatePropagation();state.form.customReminders.splice(Number(b.dataset.index),1);render()}},true);

/* global notification template */
renderNotificationSettings=function(){const s=state.settings,d=derived();return `<div class="settings-grid"><section class="settings-card span2"><h3>نمط التنبيهات الافتراضي</h3><p>يُطبق على أي عمل يختار «استخدام النمط الافتراضي»، ويمكن لكل عمل اختيار نمط مختلف أو إضافة عدة تنبيهات مخصصة.</p><div class="fields"><div class="field"><label>تشغيل التنبيهات</label><select class="select" data-setting="alertsEnabled"><option ${s.alertsEnabled!=='لا'?'selected':''}>نعم</option><option ${s.alertsEnabled==='لا'?'selected':''}>لا</option></select></div><div class="field"><label>النمط الافتراضي</label><select class="select" data-setting="defaultReminderPreset">${['day_before_due','due','three_days_due','week_due','none'].map(k=>`<option value="${k}" ${s.defaultReminderPreset===k?'selected':''}>${v330PresetLabel(k)}</option>`).join('')}</select></div><div class="field"><label>وقت التنبيه الافتراضي</label><input class="input" type="time" data-setting="defaultReminderTime" value="${esc(s.defaultReminderTime||'09:00')}"></div><div class="field"><label>تحويل المتأخر إلى «متأخرة»</label><select class="select" data-setting="alertOverdue"><option ${s.alertOverdue!=='لا'?'selected':''}>نعم</option><option ${s.alertOverdue==='لا'?'selected':''}>لا</option></select></div><div class="field span2"><div class="data-card"><div class="grow"><h4>الحالة الحالية</h4><p>تنبيهات داخل التطبيق: ${state.notifications.length} · أعمال متأخرة: ${d.overdue.length}.</p></div><button class="btn secondary" data-action="native-test-notification">اختبار إشعار Android</button></div></div><div class="field span2"><button class="btn" data-action="save-settings">حفظ إعدادات التنبيهات</button></div></div></section></div>`};

/* quick add/edit progress */
function v330OpenProgressAdd(recordId){const r=state.records.find(x=>x.id===recordId);if(!r)return;state.classicProgressEditor